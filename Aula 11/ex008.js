var vel = 25

if (vel > 60) {
    console.log('Você ultrapassou o limite de velocidade e será multado!')
} // Condição simples!!

if (vel < 30) {
    console.log('Você não está andando no limite mínimo permitido e será multado!')
}